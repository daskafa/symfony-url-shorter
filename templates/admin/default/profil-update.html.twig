{% extends 'admin/layouts/adminBase.html.twig' %}

{#{% block title %}Hello DefaultController!{% endblock %}#}

{% block body %}
    {#    {% if is_granted('ROLE_ADMIN') %} ... burası admin {% endif %}#}



    {% include 'admin/layouts/header.html.twig' %}


    <div class="bg-white">
        <div class="grid grid-cols-12">
            <div class="col-span-2">
                {% include 'admin/layouts/leftSection.html.twig' %}
            </div>
            <div class="col-span-10">
                <div class="rounded-md">



                    <div class="flex flex-col h-screen bg-gray-100">
                        <!-- Auth Card Container -->
                        <div class="grid place-items-center mx-2 my-20 sm:my-auto">
                            <!-- Auth Card -->
                            <div class="w-11/12 p-12 sm:w-8/12 md:w-6/12 lg:w-5/12 2xl:w-4/12
                                        px-6 py-10 sm:px-10 sm:py-6
                                        bg-white rounded-lg shadow-md lg:shadow-lg">

                                <!-- Card Title -->
                                <h2 class="text-center font-semibold text-3xl lg:text-4xl text-gray-800">
                                    Bilgilerini Güncelle
                                </h2>

                                <form class="mt-10" action="/admin/profil-guncelle/{{ app.user.id }}" method="POST">
                                    <!-- Email Input -->
                                    <label for="email" class="block text-xs font-semibold text-gray-600 uppercase">E-mail</label>
                                    <input id="email" type="email" name="email" placeholder="{{ app.user.username }}" autocomplete="email"
                                           class="block w-full py-3 px-1 mt-2
                                            text-gray-800 appearance-none
                                            border-b-2 border-gray-100
                                            focus:text-gray-500 focus:outline-none focus:border-gray-200"
                                           required />

                                    <!-- Password Input -->
                                    <label for="password" class="block mt-2 text-xs font-semibold text-gray-600 uppercase">Password</label>
                                    <input id="password" type="password" name="password" placeholder="password" autocomplete="current-password"
                                           class="block w-full py-3 px-1 mt-2 mb-4
                                            text-gray-800 appearance-none
                                            border-b-2 border-gray-100
                                            focus:text-gray-500 focus:outline-none focus:border-gray-200"
                                           required />

                                    <!-- Auth Buttton -->
                                    <button type="submit"
                                            class="w-full py-3 mt-10 bg-gray-800 rounded-sm
                                            font-medium text-white uppercase
                                            focus:outline-none hover:bg-gray-700 hover:shadow-none">
                                        Güncelle
                                    </button>

                                    <!-- Another Auth Routes -->
{#                                    <div class="sm:flex sm:flex-wrap mt-8 sm:mb-4 text-sm text-center">#}
{#                                        <a href="forgot-password" class="flex-2 underline">#}
{#                                            Forgot password?#}
{#                                        </a>#}

{#                                        <p class="flex-1 text-gray-500 text-md mx-4 my-1 sm:my-auto">#}
{#                                            or#}
{#                                        </p>#}

{#                                        <a href="register" class="flex-2 underline">#}
{#                                            Create an Account#}
{#                                        </a>#}
{#                                    </div>#}
                                </form>
                            </div>
                        </div>
                    </div>





















{#                    <div class="pb-12 px-56">#}

{#                        <p>Giriş Mail Adresiniz: {{ app.user.username }}</p>#}
{#                        <p>Giriş Şifreniz: {{ app.user.password }}</p>#}

{#                        <p>Güncellemek isterseniz: </p>#}
{#                        <div class="p-4 m-4">#}
{#                            <form action="/admin/profil-guncelle/{{ app.user.id }}" method="post">#}
{#                                <input type="text" name="email" class="border-2 border-blue-800">#}
{#                                <input type="password" name="password" class="border-2 border-blue-800">#}
{#                                <button type="submit">Güncelle</button>#}
{#                            </form>#}
{#                        </div>#}




{#                        {{ dump(app.user.password) }}#}
{#                    </div>#}

























                </div>
            </div>
        </div>
    </div>





    {#<div class="example-wrapper">#}


    {#    <p>Kısaltma Yapılmış URL Listesi ve Hash'leri: </p>#}
    {#    <ul>#}
    {#        {% for url in urls %}#}
    {#        <li>{{ url.url }} <p style="color: darkred">Hash: <a href="http://pa.th/{{ url.getUrlHash() }}" target="_blank">http://pa.th/{{ url.getUrlHash() }}</a></p></li>#}
    {#        {% endfor %}#}
    {#    </ul>#}
    {#    <p>Toplam Hash Sayısı: {{ urls | length }}</p>#}


    {#    <p>Kayıtlı Kullanıcılar: Toplam {{ users | length }} adet.</p>#}
    {#    <ul>#}
    {#        {% for user in users %}#}
    {#        <li>{{ user.email }}</li>#}
    {#        {% endfor %}#}
    {#    </ul>#}




    {#</div>#}
{% endblock %}
