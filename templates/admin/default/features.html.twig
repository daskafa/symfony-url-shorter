{% extends 'admin/layouts/adminBase.html.twig' %}

{#{% block title %}Hello DefaultController!{% endblock %}#}

{% block body %}
    {#    {% if is_granted('ROLE_ADMIN') %} ... burası admin {% endif %}#}



    <div class="nice-blue-bg">
        <div class="grid grid-cols-12">
            <div class="col-span-2 px-4">
                <div style="border-right: 1px solid rgb(226 226 226);" class="flex items-center h-14 border-gray-500">
                    <img src="https://cutt.ly/img/cuttly.svg" class="h-10 mx-auto" alt="">
                </div>
            </div>
            <div class="col-span-10">

            </div>
        </div>
    </div>

    <div class="bg-white">
        <div class="grid grid-cols-12">
            <div class="col-span-2 px-4">
                {% include 'admin/layouts/leftSection.html.twig' %}
            </div>
            <div class="col-span-10">
                <div class="mt-4 bg-gray-200 rounded-md">



                    <div class="pb-12 px-56">
                        <div class="grid grid-cols-3">
                            {% for features in feature %}
                            <div class="col-span-1">
                                <div class="bg-gray-100 w-3/4 mx-auto rounded-lg shadow-lg">
                                    <div class="mt-6 p-3">
                                        <img class="h-56 w-full object-cover rounded-lg shadow-2xl" src="{{ features.image }}" alt="">
                                    </div>
                                    <div class="mt-3 px-3">
                                        <h3 class="text-2xl font-bold break-words">{{ features.title }}</h3>
                                    </div>
                                    <div class="mt-3 px-3 pb-4">
                                        <p class="break-words">{{ features.description }}</p>
                                    </div>
                                </div>
                            </div>
                            {% endfor %}
                        </div>
                        <div class="grid grid-cols-3">
                            {% for features in feature %}
Umarım her konuda hayırlısı olur
                            <div class="col-span-1">
                                <div class="flex justify-center">
                                    <div class="mt-3 px-3 pb-4">
                                        <form action="/admin/ozellikler/{{ features.id }}" method="post">
                                            <input name="title" type="text" placeholder="başlık">
                                            <input name="description" type="text" placeholder="açıklama">
                                            <p class="text-center bg-gray-800 text-white p-5"><button type="submit">Güncelle</button></p>
                                        </form>
                                    </div>
                                </div>
                            </div>
                            {% endfor %}
                        </div>
                    </div>


























                </div>
            </div>
        </div>
    </div>





    {#<div class="example-wrapper">#}


    {#    <p>Kısaltma Yapılmış URL Listesi ve Hash'leri: </p>#}
    {#    <ul>#}
    {#        {% for url in urls %}#}
    {#        <li>{{ url.url }} <p style="color: darkred">Hash: <a href="http://pa.th/{{ url.getUrlHash() }}" target="_blank">http://pa.th/{{ url.getUrlHash() }}</a></p></li>#}
    {#        {% endfor %}#}
    {#    </ul>#}
    {#    <p>Toplam Hash Sayısı: {{ urls | length }}</p>#}


    {#    <p>Kayıtlı Kullanıcılar: Toplam {{ users | length }} adet.</p>#}
    {#    <ul>#}
    {#        {% for user in users %}#}
    {#        <li>{{ user.email }}</li>#}
    {#        {% endfor %}#}
    {#    </ul>#}




    {#</div>#}
{% endblock %}
